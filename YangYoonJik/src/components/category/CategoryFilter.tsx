import type {Category} from '../../types/data';
interface Props{categories:Category[];categoryIndex:number;analysisKey:string;onCategory:(index:number)=>void;onIndustry:(key:string)=>void}
export function CategoryFilter({categories,categoryIndex,analysisKey,onCategory,onIndustry}:Props){const current=categories[categoryIndex];return <section className="filters" aria-label="업종 선택"><div><label htmlFor="category">상위 업종</label><select id="category" value={categoryIndex} onChange={e=>onCategory(Number(e.target.value))}>{categories.map((c,i)=><option key={c.analysisKey} value={i}>{c.name}</option>)}</select></div><div><label htmlFor="industry">하위 업종</label><select id="industry" value={analysisKey} onChange={e=>onIndustry(e.target.value)}>{current?.children.map(c=><option key={c.analysisKey} value={c.analysisKey}>{c.name}</option>)}</select></div><p>분석 지역 <strong>서울특별시 송파구</strong></p></section>}
